# Scripts here run on Linux (galgo HPC / WSL); force LF regardless of
# core.autocrlf on Windows checkouts, to avoid "$'\r': command not found".
*.sh    text eol=lf
*.slurm text eol=lf
*.py    text eol=lf
